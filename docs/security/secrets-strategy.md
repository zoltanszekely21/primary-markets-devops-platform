# Secrets Strategy

## Baseline (week 1)
- Kubernetes secrets for Datadog API key in `datadog` namespace.

## Target (later)
- AWS Secrets Manager + External Secrets Operator
- KMS encryption for secrets at rest
- Rotation process documented in runbooks
